import React from 'react';
import { Box } from '@mui/system';
import theme from '../../theme/Theme';

export default function NormalCard() {
    return (
        <Box sx={{padding: 20, background: theme.white} }></Box>
    )
}
