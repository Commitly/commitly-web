
import styled from 'styled-components';

const S = {
    container: styled.div`
        display: flex;
        flex-direction: column;
        align-items: start;
        padding-top: 120px;
        padding-left: 100px;
    `,
}

export default S;